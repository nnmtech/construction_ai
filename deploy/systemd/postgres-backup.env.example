# Optional overrides for scripts/postgres-backup.sh
# Copy to: /opt/construction_ai_landing/deploy/systemd/postgres-backup.env

# Where backups are written on the host
BACKUP_DIR=/opt/construction_ai_backups/postgres

# Retention in days
KEEP_DAYS=14

# Compose project name used by the production stack
# (Only change if you deployed with a different -p/COMPOSE_PROJECT_NAME)
PROJECT=construction_ai_landing_prod

# If your deployment uses different locations, override these:
# COMPOSE_FILE=/opt/construction_ai_landing/docker-compose.prod.yml
# ENV_FILE=/opt/construction_ai_landing/.env.prod
